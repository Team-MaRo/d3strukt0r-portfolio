node_modules/
build/
.react-router/

# Cloudflare Workers — local wrangler state + generated worker types.
.wrangler
.dev.vars*
worker-configuration.d.ts
.mf

# Nix build outputs — host-specific symlinks into /nix/store, never useful to commit.
result
result-*
image.tar

.cache/
.env
.DS_Store
*.log
.pnpm-store/
.claude/settings.local.json
.claude/*.lock
CLAUDE.md

# LinkedIn export — entire directory is local-only. PII / private conversations /
# ID verification / behavioral data live here; CI uses the API path
# (`pnpm run sync:linkedin:api`), not the CSV one.
data/linkedin/
